import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <div className={styles.footer}>
      <h5 className={styles.title}>Staycation</h5>
    </div>
  );
};

export default Footer;
